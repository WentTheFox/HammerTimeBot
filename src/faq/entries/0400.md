# Can you automatically detect my timezone so I don't have to specify it each time?
On the site I have a lot more information to work with, e.g. I can get the system timezone so you don't have to specify it manually. Discord does not store nor send user timezone data when users execute slash commands, so in order to not have to specify this each time you need to change your user settings for <@964106782790283295> by either
1. using the </settings:1231738209369587762> command to view your settings page directly, or
2. by logging in on the <https://hammertime.cyou> website manually.
Simply select your preferred timezone and it will be used as the default any time you do not specify one.
